use crate::actuators::{SharedClimateActuator,SharedMediaActuator};
use crate::command_executor::{CommandExecutionError,CommandExecutor};
use crate::commands;
use crate::producer::{SharedHub,SharedRuntime};
use futures_util::{SinkExt,StreamExt};
use radome_core::{Capability,Client,Envelope,MessageType,Role};
use serde_json::{json,Value};
use std::sync::atomic::{AtomicU64,Ordering};use std::sync::Arc;
use tokio::net::{TcpListener,TcpStream};use tokio::sync::mpsc;use tokio_tungstenite::{accept_async,tungstenite::Message};
static NEXT_SESSION_ID:AtomicU64=AtomicU64::new(1);static NEXT_SERVER_MESSAGE_ID:AtomicU64=AtomicU64::new(1);
#[derive(Debug,Default)]struct ConnectionSession{id:Option<String>,client_id:Option<String>,registered:bool}
impl ConnectionSession{fn is_established(&self)->bool{self.id.is_some()}fn establish(&mut self,client_id:String)->String{let sequence=NEXT_SESSION_ID.fetch_add(1,Ordering::Relaxed);let id=format!("session-{sequence}");self.id=Some(id.clone());self.client_id=Some(client_id);id}}
fn server_id(prefix:&str)->String{format!("server-{prefix}-{}",NEXT_SERVER_MESSAGE_ID.fetch_add(1,Ordering::Relaxed))}
pub async fn serve(listener:TcpListener,runtime:SharedRuntime,hub:SharedHub,climate_actuator:SharedClimateActuator,media_actuator:SharedMediaActuator)->Result<(),Box<dyn std::error::Error>>{loop{let(stream,peer)=listener.accept().await?;let runtime=Arc::clone(&runtime);let hub=Arc::clone(&hub);let climate_actuator=Arc::clone(&climate_actuator);let media_actuator=Arc::clone(&media_actuator);tokio::spawn(async move{if let Err(error)=handle_connection(stream,runtime,hub,climate_actuator,media_actuator).await{eprintln!("connection {peer} closed with error: {error}");}});}}
async fn handle_connection(stream:TcpStream,runtime:SharedRuntime,hub:SharedHub,climate_actuator:SharedClimateActuator,media_actuator:SharedMediaActuator)->Result<(),Box<dyn std::error::Error+Send+Sync>>{let websocket=accept_async(stream).await?;let(mut sink,mut source)=websocket.split();let(outbound_tx,mut outbound_rx)=mpsc::unbounded_channel::<Envelope>();let writer=tokio::spawn(async move{while let Some(envelope)=outbound_rx.recv().await{sink.send(Message::Text(envelope.encode_json()?.into())).await?;}Ok::<(),Box<dyn std::error::Error+Send+Sync>>(())});let executor=CommandExecutor::new(climate_actuator,media_actuator);let mut session=ConnectionSession::default();while let Some(message)=source.next().await{let message=message?;match message{Message::Text(text)=>{let responses=match Envelope::decode_json(text.as_ref()){Ok(incoming)=>handle_envelope(&mut session,&runtime,&hub,&outbound_tx,&executor,incoming),Err(error)=>vec![Envelope::new("server-error",MessageType::Error,json!({"reason":format!("{error:?}")}))]};for response in responses{outbound_tx.send(response).map_err(|_|"websocket writer closed")?;}},Message::Close(_)=>break,_=>{}}}unregister_session(&session,&runtime,&hub);drop(outbound_tx);writer.await??;Ok(())}
fn handle_envelope(session:&mut ConnectionSession,runtime:&SharedRuntime,hub:&SharedHub,outbound:&mpsc::UnboundedSender<Envelope>,executor:&CommandExecutor,incoming:Envelope)->Vec<Envelope>{let response=match incoming.message_type{MessageType::Hello if !session.is_established()=>handle_hello(session,incoming),MessageType::Hello=>error_for(&incoming,"session_already_established"),MessageType::CapabilityAnnounce if !session.is_established()=>error_for(&incoming,"hello_required"),MessageType::CapabilityAnnounce=>handle_capability_announce(session,runtime,hub,outbound,incoming),MessageType::DiscoveryRequest if !session.is_established()=>error_for(&incoming,"hello_required"),MessageType::DiscoveryRequest=>handle_discovery(session,incoming),MessageType::StateSnapshotRequest if !session.is_established()=>error_for(&incoming,"hello_required"),MessageType::StateSnapshotRequest=>handle_state_snapshot(session,executor,incoming),MessageType::Command if !session.is_established()=>error_for(&incoming,"hello_required"),MessageType::Command=>return handle_command(session,runtime,executor,incoming),_ if !session.is_established()=>error_for(&incoming,"hello_required"),_=>error_for(&incoming,"unsupported_message_type")};vec![response]}
fn handle_discovery(session:&ConnectionSession,incoming:Envelope)->Envelope{if incoming.session_id.as_deref()!=session.id.as_deref(){return error_for(&incoming,"invalid_session");}let commands=commands::all().into_iter().map(|command|json!({"name":command.name,"required_capability":command.required_capability.name(),"event_name":command.event_name})).collect::<Vec<_>>();let mut capabilities=commands.iter().filter_map(|command|command["required_capability"].as_str()).collect::<Vec<_>>();capabilities.sort_unstable();capabilities.dedup();Envelope::new(server_id("discovery"),MessageType::DiscoveryResult,json!({"capabilities":capabilities,"commands":commands})).correlated_to(incoming.id).in_session(session.id.clone().expect("established session"))}
fn handle_state_snapshot(session:&ConnectionSession,executor:&CommandExecutor,incoming:Envelope)->Envelope{if incoming.session_id.as_deref()!=session.id.as_deref(){return error_for(&incoming,"invalid_session");}Envelope::new(server_id("state-snapshot"),MessageType::StateSnapshot,executor.state_snapshot()).correlated_to(incoming.id).in_session(session.id.clone().expect("established session"))}
fn handle_command(session:&ConnectionSession,runtime:&SharedRuntime,executor:&CommandExecutor,incoming:Envelope)->Vec<Envelope>{if incoming.session_id.as_deref()!=session.id.as_deref(){return vec![error_for(&incoming,"invalid_session")];}if !session.registered{return vec![error_for(&incoming,"capability_announce_required")];}let Some(name)=incoming.payload.get("name").and_then(Value::as_str)else{return vec![error_for(&incoming,"missing_command_name")];};let client_id=session.client_id.as_deref().expect("established session client id");let data=incoming.payload.get("data").unwrap_or(&Value::Null);match executor.execute(name,data,|capability|runtime.lock().expect("runtime mutex poisoned").client_can(client_id,capability)){Ok(success)=>{let result=command_result(&incoming,"succeeded","accepted");let event=Envelope::new(server_id("event"),MessageType::Event,json!({"name":success.event_name,"data":success.event_data})).in_session(session.id.clone().expect("established session"));vec![result,event]},Err(CommandExecutionError::UnsupportedCommand)=>vec![command_result(&incoming,"failed","unsupported_command")],Err(CommandExecutionError::CapabilityDenied)=>vec![command_result(&incoming,"failed","capability_denied")],Err(CommandExecutionError::ActuatorRejected)=>vec![command_result(&incoming,"failed","actuator_rejected")],Err(CommandExecutionError::InvalidPayload{code,detail})=>vec![command_result_payload(&incoming,"failed",json!({"code":code,"detail":detail}))]}}
fn command_result(incoming:&Envelope,outcome:&str,data:&str)->Envelope{command_result_payload(incoming,outcome,json!(data))}fn command_result_payload(incoming:&Envelope,outcome:&str,data:Value)->Envelope{let mut result=Envelope::new(server_id("command-result"),MessageType::CommandResult,json!({"outcome":outcome,"data":data})).correlated_to(incoming.id.clone());if let Some(session_id)=incoming.session_id.clone(){result=result.in_session(session_id);}result}
fn handle_hello(session:&mut ConnectionSession,incoming:Envelope)->Envelope{let Some(client_id)=incoming.payload.get("client_id").and_then(Value::as_str)else{return error_for(&incoming,"missing_client_id")};let session_id=session.establish(client_id.to_owned());Envelope::new("server-hello",MessageType::Hello,json!({"server":"radome-server","protocol_version":radome_core::PROTOCOL_VERSION})).correlated_to(incoming.id).in_session(session_id)}
fn handle_capability_announce(session:&mut ConnectionSession,runtime:&SharedRuntime,hub:&SharedHub,outbound:&mpsc::UnboundedSender<Envelope>,incoming:Envelope)->Envelope{if incoming.session_id.as_deref()!=session.id.as_deref(){return error_for(&incoming,"invalid_session");}let Some(role)=incoming.payload.get("role").and_then(Value::as_str)else{return error_for(&incoming,"missing_role")};let Some(values)=incoming.payload.get("capabilities").and_then(Value::as_array)else{return error_for(&incoming,"missing_capabilities")};let Some(capabilities)=values.iter().map(Value::as_str).collect::<Option<Vec<_>>>()else{return error_for(&incoming,"invalid_capabilities")};let client_id=session.client_id.clone().expect("established session has client id");runtime.lock().expect("runtime mutex poisoned").register_client(Client::new(client_id.clone(),Role::new(role),capabilities.into_iter().map(Capability::new)));hub.lock().expect("hub mutex poisoned").register(client_id.clone(),outbound.clone());session.registered=true;Envelope::new("capabilities-accepted",MessageType::CapabilityAnnounce,json!({"accepted":true,"client_id":client_id})).correlated_to(incoming.id).in_session(session.id.clone().expect("established session"))}
fn unregister_session(session:&ConnectionSession,runtime:&SharedRuntime,hub:&SharedHub){if session.registered{if let Some(client_id)=session.client_id.as_deref(){runtime.lock().expect("runtime mutex poisoned").unregister_client(client_id);hub.lock().expect("hub mutex poisoned").unregister(client_id);}}}fn error_for(incoming:&Envelope,reason:&str)->Envelope{let mut response=Envelope::new("server-error",MessageType::Error,json!({"reason":reason})).correlated_to(incoming.id.clone());if let Some(session_id)=incoming.session_id.clone(){response=response.in_session(session_id);}response}

#[cfg(test)]mod tests{use super::*;use crate::actuators::{DemoClimateActuator,DemoMediaActuator};use crate::hub::ConnectionHub;use radome_core::runtime::Runtime;use radome_core::SystemCapabilities;use tokio::time::{timeout,Duration};use tokio_tungstenite::connect_async;
fn runtime()->SharedRuntime{Arc::new(std::sync::Mutex::new(Runtime::new(SystemCapabilities::new([Capability::new("vehicle.telemetry")]))))}fn hub()->SharedHub{Arc::new(std::sync::Mutex::new(ConnectionHub::default()))}fn climate()->SharedClimateActuator{Arc::new(DemoClimateActuator::new())}fn media()->SharedMediaActuator{Arc::new(DemoMediaActuator::new())}
async fn send<S>(socket:&mut tokio_tungstenite::WebSocketStream<S>,envelope:Envelope)where S:tokio::io::AsyncRead+tokio::io::AsyncWrite+Unpin{socket.send(Message::Text(envelope.encode_json().unwrap().into())).await.unwrap();}async fn receive<S>(socket:&mut tokio_tungstenite::WebSocketStream<S>)->Envelope where S:tokio::io::AsyncRead+tokio::io::AsyncWrite+Unpin{let message=timeout(Duration::from_secs(2),socket.next()).await.expect("server response timeout").expect("socket closed").expect("websocket error");let Message::Text(text)=message else{panic!("expected text websocket message")};Envelope::decode_json(text.as_ref()).unwrap()}
async fn fixture()->(tokio_tungstenite::WebSocketStream<tokio_tungstenite::MaybeTlsStream<TcpStream>>,tokio::task::JoinHandle<()>){let listener=TcpListener::bind("127.0.0.1:0").await.unwrap();let addr=listener.local_addr().unwrap();let server=tokio::spawn(async move{let(stream,_)=listener.accept().await.unwrap();handle_connection(stream,runtime(),hub(),climate(),media()).await.unwrap();});let(socket,_)=connect_async(format!("ws://{addr}")).await.unwrap();(socket,server)}
async fn hello(socket:&mut tokio_tungstenite::WebSocketStream<tokio_tungstenite::MaybeTlsStream<TcpStream>>)->String{send(socket,Envelope::new("hello-test",MessageType::Hello,json!({"client_id":"integration-console"}))).await;receive(socket).await.session_id.unwrap()}
async fn establish(socket:&mut tokio_tungstenite::WebSocketStream<tokio_tungstenite::MaybeTlsStream<TcpStream>>,capabilities:Value)->String{let session_id=hello(socket).await;send(socket,Envelope::new("caps-test",MessageType::CapabilityAnnounce,json!({"role":"center-console","capabilities":capabilities})).in_session(session_id.clone())).await;assert_eq!(receive(socket).await.payload["accepted"],true);session_id}
#[tokio::test]async fn client_bootstraps_from_discovery_without_hard_coded_server_capabilities(){let(mut socket,server)=fixture().await;let session_id=hello(&mut socket).await;send(&mut socket,Envelope::new("bootstrap-discovery",MessageType::DiscoveryRequest,json!({})).in_session(session_id.clone())).await;let discovery=receive(&mut socket).await;assert_eq!(discovery.message_type,MessageType::DiscoveryResult);let offered=discovery.payload["capabilities"].as_array().unwrap();let supported_by_client=["media.control"];let selected=offered.iter().filter_map(Value::as_str).filter(|capability|supported_by_client.contains(capability)).collect::<Vec<_>>();assert_eq!(selected,vec!["media.control"]);send(&mut socket,Envelope::new("bootstrap-capabilities",MessageType::CapabilityAnnounce,json!({"role":"center-console","capabilities":selected})).in_session(session_id.clone())).await;let accepted=receive(&mut socket).await;assert_eq!(accepted.payload["accepted"],true);send(&mut socket,Envelope::new("bootstrap-snapshot",MessageType::StateSnapshotRequest,json!({})).in_session(session_id.clone())).await;let snapshot=receive(&mut socket).await;assert_eq!(snapshot.message_type,MessageType::StateSnapshot);assert_eq!(snapshot.payload["media"],json!({"playback":"paused","volume":50,"track_index":0}));send(&mut socket,Envelope::new("bootstrap-command",MessageType::Command,json!({"name":"media.play"})).in_session(session_id)).await;let result=receive(&mut socket).await;let event=receive(&mut socket).await;assert_eq!(result.payload["outcome"],"succeeded");assert_eq!(event.payload["name"],"media.playback_started");assert_eq!(event.payload["data"]["playback"],"playing");socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn snapshot_after_commands_matches_the_last_emitted_states(){let(mut socket,server)=fixture().await;let session_id=establish(&mut socket,json!(["climate.control","media.control"])).await;send(&mut socket,Envelope::new("cmd-climate-sync",MessageType::Command,json!({"name":"climate.set_temperature","data":{"temperature_c":23.0}})).in_session(session_id.clone())).await;assert_eq!(receive(&mut socket).await.payload["outcome"],"succeeded");let climate_event=receive(&mut socket).await;assert_eq!(climate_event.payload["data"],json!({"temperature_c":23.0}));send(&mut socket,Envelope::new("cmd-media-sync",MessageType::Command,json!({"name":"media.set_volume","data":{"volume":72}})).in_session(session_id.clone())).await;assert_eq!(receive(&mut socket).await.payload["outcome"],"succeeded");let media_event=receive(&mut socket).await;assert_eq!(media_event.payload["data"],json!({"playback":"paused","volume":72,"track_index":0}));send(&mut socket,Envelope::new("snapshot-after-commands",MessageType::StateSnapshotRequest,json!({})).in_session(session_id)).await;let snapshot=receive(&mut socket).await;assert_eq!(snapshot.payload["climate"],climate_event.payload["data"]);assert_eq!(snapshot.payload["media"],media_event.payload["data"]);socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn websocket_client_can_request_state_snapshot_after_hello(){let(mut socket,server)=fixture().await;let session_id=hello(&mut socket).await;send(&mut socket,Envelope::new("snapshot-test",MessageType::StateSnapshotRequest,json!({})).in_session(session_id.clone())).await;let result=receive(&mut socket).await;assert_eq!(result.message_type,MessageType::StateSnapshot);assert_eq!(result.correlation_id.as_deref(),Some("snapshot-test"));assert_eq!(result.session_id.as_deref(),Some(session_id.as_str()));assert_eq!(result.payload,json!({"climate":{"temperature_c":20.0},"media":{"playback":"paused","volume":50,"track_index":0}}));socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn websocket_client_can_discover_commands_after_hello(){let(mut socket,server)=fixture().await;let session_id=hello(&mut socket).await;send(&mut socket,Envelope::new("discover-test",MessageType::DiscoveryRequest,json!({})).in_session(session_id.clone())).await;let result=receive(&mut socket).await;assert_eq!(result.message_type,MessageType::DiscoveryResult);assert_eq!(result.correlation_id.as_deref(),Some("discover-test"));assert_eq!(result.session_id.as_deref(),Some(session_id.as_str()));assert_eq!(result.payload["capabilities"],json!(["climate.control","media.control"]));assert_eq!(result.payload["commands"].as_array().unwrap().len(),9);assert!(result.payload["commands"].as_array().unwrap().iter().any(|command|command["name"]=="media.play"&&command["required_capability"]=="media.control"));socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn websocket_client_can_execute_climate_command_end_to_end(){let(mut socket,server)=fixture().await;let session_id=establish(&mut socket,json!(["climate.control"])).await;send(&mut socket,Envelope::new("cmd-climate",MessageType::Command,json!({"name":"climate.set_temperature","data":{"temperature_c":21.5}})).in_session(session_id)).await;assert_eq!(receive(&mut socket).await.payload["outcome"],"succeeded");assert_eq!(receive(&mut socket).await.payload["name"],"climate.temperature_changed");socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn websocket_client_can_execute_media_command_end_to_end(){let(mut socket,server)=fixture().await;let session_id=establish(&mut socket,json!(["media.control"])).await;send(&mut socket,Envelope::new("cmd-media",MessageType::Command,json!({"name":"media.next_track"})).in_session(session_id.clone())).await;let result=receive(&mut socket).await;let event=receive(&mut socket).await;assert_eq!(result.payload["outcome"],"succeeded");assert_eq!(result.session_id.as_deref(),Some(session_id.as_str()));assert_eq!(event.session_id.as_deref(),Some(session_id.as_str()));assert_eq!(event.payload["name"],"media.next_track_requested");assert_eq!(event.payload["data"],json!({"playback":"paused","volume":50,"track_index":1}));socket.close(None).await.unwrap();server.await.unwrap();}
#[tokio::test]async fn websocket_denied_command_emits_no_domain_event(){let(mut socket,server)=fixture().await;let session_id=establish(&mut socket,json!([])).await;send(&mut socket,Envelope::new("cmd-denied",MessageType::Command,json!({"name":"climate.set_temperature","data":{"temperature_c":21.5}})).in_session(session_id)).await;assert_eq!(receive(&mut socket).await.payload["data"],"capability_denied");assert!(timeout(Duration::from_millis(100),socket.next()).await.is_err());socket.close(None).await.unwrap();server.await.unwrap();}}